# -*- coding: utf-8 -*-
"""sm-6.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1mcrPadYuCyli5jXxdsgXR2ZTkl9h8U2T
"""

import numpy as np 
import pandas as pd 
from google.colab import files
uploaded=files.upload()

# Commented out IPython magic to ensure Python compatibility.
# %matplotlib inline
import matplotlib.pyplot as plt
import seaborn as sns
import statsmodels.api as sm
da=pd.read_csv("fifa.csv")

da.head()

da.shape

da.info()

da.describe()

da.columns=["Position","Team","Games_Played","Win","Draw","Loss","Goals_For","Goals_Against","Goal_Difference","Points"]

da.head()

da.shape

duplicateRowsDF=da[da.duplicated()]
duplicateRowsDF

da=da.drop([296])

da

duplicateRowsDF=da[da.duplicated()]
duplicateRowsDF

da.head()

da.isnull().sum()

da.shape

da.dropna(inplace=True)

da.shape

numerical=["Win","Draw","Loss","Goal_Difference"]
da[numerical].describe()

fig, axs = plt.subplots(2,2,figsize=(15,10))
sns.histplot(data=da,x="Win",kde=True,color="yellow",ax=axs[0,0])
sns.histplot(data=da,x="Draw",kde=True,color="blue",ax=axs[0,1])
sns.histplot(data=da,x="Loss",kde=True,color="pink",ax=axs[1,0])
sns.histplot(data=da,x="Points",kde=True,color="gold",ax=axs[1,1])

da=da.replace(to_replace="âˆ’",value=0,regex="True")

fig, axs = plt.subplots(2,2,figsize=(15,8))
sns.boxplot(da['Win'],ax=axs[0,0])
sns.boxplot(da['Draw'],ax=axs[0,1])
sns.boxplot(da['Loss'],ax=axs[1,0])
sns.boxplot(da['Points'],ax=axs[1,1])